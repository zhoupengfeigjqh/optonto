概念问题：
1 实例化对象务必有primarykey
2 Object Set：对象集合，实例化集合如何构建，查询结果存储？
3 函数计算和模型计算：都合并到行为API来计算
4 对象的状态生命周期记录：概念属性的status？
5 权限管控：行为human-in-loop
6 LLM Tool工具：调用执行动作

建模：
对象建模
关系建模
数据映射：如何将业务系统与本体字段进行映射（与现成的API接口参数进行映射，或重新开发直接对齐），如何构建和管理数据对象集（操作过程中的对象如何创建/更新/管理）
动作建模：行为-规则-安全
数据治理：？
AI介入：

思想：语义对齐、流程逻辑、行动闭环、决策对象化（AI不直接接触底层数据）、Agent面向行为操作、对象优先/关系显式/权限内生，，

数据源接入：源系统映射、实体解析、PipeLine
对象同步：数据血缘、增量刷新、质量校验
语义对齐：ID映射、主数据、口径统一


AIP与Agent
LLM Tool将action接口暴露给Agent，action包括读写/函数计算/模型计算


数据映射问题：
# 边界说明文档（节选）
字段: supplier_name
  源数据映射: erp_vendor.legal_name, po_supplier.name_alias
  语义归属: 业务对象.身份属性（主名称）
  指标计算: 不涉及
  应用呈现: 详情页加粗显示，列表页截断为20字符
字段: total_spend
  源数据映射: 无（不存储）
  语义归属: 无
  指标计算: 聚合函数（SUM(po_order.amount) WHERE status='已完成'）
  应用呈现: 财务看板显示为货币格式，带同比箭头

注：数据映射是在数据引擎里；通过数据映射，当物理表发生变化时，我只需改映射，上层看到的对象不变！

指标计算合并到API接口里，不单独写？是否增加一个函数，用来计算某些概念的某些属性值或统计值？如计算100个对象的均值等。

下一步：增加function计算函数，作为skill的附属参考，用于agent调用

【参考模板格式（严格遵循）】
- 输入参数: {{"参数名": {{"type": "string", "description": "参数描述", "required": true, "example": "示例值"}}}}
- 输出结构: {{"字段名": {{"type": "string", "description": "字段描述", "example": "示例值"}}}}
- 嵌套对象用 properties: {{"data": {{"type": "object", "properties": {{"id": {{"type": "string", "description": "...", "example": "..."}}}}}}}}
- 数组类型: {{"items": {{"type": "array", "items": {{"type": "object", "properties": {{...}}}}}}}}

