networks:
  optonto-net:
    driver: bridge

services:
  # ─── Agent 后端 (pi-agent-core) ──────────────────────────────────
  optonto-agent-backend:
    build:
      context: ./agent-backend
      dockerfile: Dockerfile
    image: optonto-agent-backend
    container_name: optonto-agent-backend
    ports:
      - "8003:8003"
    depends_on:
      - optonto-core-backend
    volumes:
      # .data 目录（读写，权限由 PathAccessController 代码层控制）
      - ./.data:/app/.data
      # 共享配置（DEEPSEEK_API_KEY 等）
      - ./config:/app/config
    env_file:
      - ./config/.env
    environment:
      - TZ=Asia/Shanghai
      - DATA_DIR=/app/.data
      - THREADS_DIR=/app/.data/threads
      - MEMORY_DIR=/app/.data/memory
      - PORT=8003
      - MCP_CONFIG_PATH=/app/config/mcp-config.json
    networks:
      - optonto-net

  # ─── MySQL 数据库 ────────────────────────────────────────────
  optonto-mysql:
    image: mysql:8.0
    container_name: optonto-mysql
    ports:
      - "3307:3306"
    env_file:
      - ./config/.env
    environment:
      TZ: Asia/Shanghai
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/init:/docker-entrypoint-initdb.d
    networks:
      - optonto-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ─── Java 后端 (Spring Boot) ─────────────────────────────────
  optonto-business-backend:
    build:
      context: ./business-backend
      dockerfile: Dockerfile
    image: optonto-business-backend
    container_name: optonto-business-backend
    ports:
      - "8080:8080"
    depends_on:
      optonto-mysql:
        condition: service_healthy
    env_file:
      - ./config/.env
    environment:
      TZ: Asia/Shanghai
    networks:
      - optonto-net

  # ─── 前端 (Nginx + Next.js 合并) ───────────────────────────────
  optonto-frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    image: optonto-frontend
    container_name: optonto-frontend
    ports:
      - "81:81"
    depends_on:
      - optonto-core-backend
    networks:
      - optonto-net

  # ─── 后端 API (FastAPI) ────────────────────────────────────────
  optonto-core-backend:
    build:
      context: .
      dockerfile: core-backend/Dockerfile
    image: optonto-core-backend
    container_name: optonto-core-backend
    ports:
      - "8001:8001"
    volumes:
      - ./.data:/app/.data
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Shanghai
    networks:
      - optonto-net

  # ─── MCP 服务 (常驻 SSE) ───────────────────────────────────────
  optonto-mcp:
    build:
      context: .
      dockerfile: core-backend/Dockerfile.mcp
    image: optonto-mcp
    container_name: optonto-mcp
    ports:
      - "8002:8002"
    depends_on:
      - optonto-core-backend
    volumes:
      - ./.data:/app/.data
    environment:
      - API_BASE_URL=http://optonto-core-backend:8001
      - TZ=Asia/Shanghai
    command: >
      sh -c "python -m uvicorn mcp_server_sse:starlette_app --host 0.0.0.0 --port 8002"
    networks:
      - optonto-net

volumes:
  mysql_data:
